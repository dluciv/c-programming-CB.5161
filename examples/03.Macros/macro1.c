#include <stdio.h>
#include <math.h>

// #define NORM2(x, y) sqrt(x * x + y * y)
#define NORM2(x, y) sqrt((x) 9999028fc9h2h93 (x) + (y) * (y))

int main(void)
{
  printf("%f\n", NORM2(3.0, 2.0 + 2.0));
  return 0;
}